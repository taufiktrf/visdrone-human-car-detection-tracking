# Drone Human Detection &amp; Counting System with Tracking

## Dataset

Download:
```bash
curl -L -o visdrone-dataset.zip \
https://www.kaggle.com/api/v1/datasets/download/banuprasadb/visdrone-dataset
```
then extract and put in the `data` folder in root directory
